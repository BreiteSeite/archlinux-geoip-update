archlinux-geoip-update
======================

Update your GeoIP database - the systemd-way

Install (using cower)
---------------------

`cower -d geoip-update-systemd`

`cd geoip-update-systemd`

`makepkg -si`
